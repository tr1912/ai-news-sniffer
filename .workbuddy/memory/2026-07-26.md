# 2026-07-26

## DeepSeek API 调用失败排查
- 模型名 `deepseek-v4-flash` 确认正确（DeepSeek 2026/07 最新 API 文档）
- 旧模型 `deepseek-chat` / `deepseek-reasoner` 已于 2026/07/24 弃用
- API 端点 `https://api.deepseek.com` 正常，`response_format` 参数兼容
- OpenAI SDK 2.48.0 兼容
- 排查结论：大概率是 GitHub Secrets 里 `DEEPSEEK_API_KEY` 失效或账户余额不足
- 用户确认余额充足（¥19.50）、Build digest 无报错，但页面仍显示 0 条精选 + 降级模式
- run_id 哈希 `e3b0c442` 对应空 event_key，说明最终 events 为空
- 可能原因：模型调用失败进入降级模式，且 build_degraded_events 仅保留 OFFICIAL/RESEARCH 来源，若候选全为 COMMUNITY 则结果为空
- 已增强可观测性：pipeline.py 把 warnings 和 summary 打印到 stderr；workflow 增加 report JSON dump 步骤
- 需要用户重新跑一次 workflow，查看 Build digest 中的 `[warning]` 和 report JSON 定位具体失败原因

## 修复 notify --force 对 NOTIFIED 记录的守卫逻辑

问题：手动触发 workflow_dispatch 会带 `--force`，但今天已经 NOTIFIED 后，
notify 命令第一个 guard 被 bypass，第二个 guard 只接受 PUBLISHED/PARTIALLY_NOTIFIED，
于是报错 `RuntimeError: run must be published before notification`。

修复：`cli.py notify` 在 `--force` 时把 `RunStatus.NOTIFIED` 也加入允许集合。

## 2026-07-26 运行根因确认与修复

根因：
- source-health.json 显示大量 official/research 源因解析失败被 auto-paused（7 次连续失败）。
- 本次 build 仅有 9 个源启用，且 AI 候选里无 official/research 文章。
- 模型返回的事件未通过来源验证（unverified event），编辑层整批失败并进入降级模式。
- 降级模式 `build_degraded_events` 只保留 OFFICIAL/RESEARCH，导致 events 为空，run_id 哈希为 `e3b0c442`（空字符串 SHA256）。

修复内容：
1. `src/ai_news_sniffer/sources/html.py`：增强日期解析（ISO naive、常见格式、RFC 2822），处理 `@type` 列表与嵌套 `@graph`，选择器支持 `time` 文本内容回退。
2. `src/ai_news_sniffer/providers/editorial.py`：单个事件验证失败时仅丢弃该事件，而非整批失败；全部无效时才抛异常触发降级。
3. `src/ai_news_sniffer/source_service.py`：auto-paused 源超过 24 小时自动尝试一次，成功则恢复。
4. `pyproject.toml`：新增 `tzdata` 依赖，保证 Windows 上 `ZoneInfo` 可用。
5. 更新相关测试：naive 日期、@type 列表、编辑服务丢弃无效事件、暂停源重试逻辑。

- 文件：html.py, editorial.py, source_service.py, pyproject.toml, tests/test_providers.py, tests/test_source_adapters.py, tests/test_source_service.py
- 75/75 tests pass，ruff clean
- 下一次 workflow 运行时会自动重试已暂停的 official 源；若解析修复生效，源会恢复并产出合格候选。
- 本地提交：c1e2004、af7e67d、ad9ca24；push 因本地无 GitHub 凭证失败，需用户手动 push 或提供 PAT。
