# 2026-07-24

## 通知模板优化

- `site.py` render_notification 新增 `report_url` 参数，透传 base_path 到模板
- `cli.py` 从 public_base_url 提取 base_path，初始化 SiteRenderer 时传入
- `notification.md.j2` 重设计：emoji 头部、纯 URL 链接（避免 MeoW 换行）、底部"查看完整日报"
- 提交：`a9e1e27 feat: refine MeoW notification template`

## GitHub Actions 故障修复

- 修复 `.github/workflows/daily-digest.yml` 中 `runtime-data` 孤儿分支创建步骤：
  - `git rm -rf .` 在孤儿分支无跟踪文件时会失败，改为 `|| true`
  - 增加 `git clean -fdx || true` 清除工作树中残留文件
- 提交：`7961cc5 ci: fix runtime-data orphan branch creation in daily-digest workflow`

## UI 全面重设计

- 修复 CSS 路径 bug：所有模板改用 `{{ base_path }}` 前缀，从 `public_base_url` URL 路径提取
  - `SiteRenderer.__init__` 新增 `base_path` 参数
  - `Pipeline.__init__` 从 settings 解析 base_path
- CSS 全面重写：
  - Hero 深紫渐变 (`#1a0533 → #4c1d95`) + 径向光晕叠加
  - 标题渐变色文字效果
  - 文章卡片：白色圆角阴影 + hover 微抬升 + 紫色边框过渡
  - 彩色分类徽章（research=蓝、models=绿、products=橙、tools=粉、community=红）
  - 阅读原文 pill 按钮：紫色渐变 + hover 缩放阴影
  - 来源覆盖：折叠式统计卡片 + 报错默认隐藏
  - 中文字体栈：PingFang SC / Noto Sans SC / Microsoft YaHei
  - 内联 SVG favicon
  - OpenGraph 社交预览标签
- 3 个模板全部适配更新
- 测试适配：更新 `test_rendering.py` 断言匹配新统计格式
- 提交：`fb31182 feat: full UI redesign`
- 75/75 测试通过


## Main Task 5: Provider Chain & Semantic Editorial Pass — 完成

- 创建 `src/ai_news_sniffer/providers/__init__.py`、`base.py`、`openai_compatible.py`、`editorial.py`
- 实现 `StructuredProvider` 协议和 `ProviderChain` 退避链（支持多供应商备用切换）
- 实现 `OpenAICompatibleProvider`：基于 OpenAI SDK，封装重试 + 指数退避，支持 DeepSeek/Kimi/MiniMax
- 实现 `EditorialService`：候选序列化 → 模型调用 → Pydantic 结构化校验 → NewsEvent 转换，含事实确认和来源校验
- 创建 `prompts/editorial.md` 版本化编辑提示（中文 AI 日报编辑指令）
- 添加 `tests/test_providers.py`（8 个测试）和 `tests/fixtures/editorial_response.json`
- 全量测试：61 passed, ruff clean

## 项目状态
- 来源子系统 (Source Tasks 1-7): 全部完成
- Main Task 4 (Runtime State): 已完成
- Main Task 5 (Provider Chain): 刚完成
- 待完成: Main Task 10（GitHub Actions CI/CD）

## Main Task 10: GitHub Actions + CI/CD + Docs — 完成

- 创建 `.github/workflows/ci.yml`: push/PR 触发，lint+test+coverage
- 创建 `.github/workflows/daily-digest.yml`: 每日 13:00 UTC 定时 + manual dispatch，8 个输入参数（dry_run/publish/notify/target_date/source_profile/include_sources/exclude_sources/max_ai_candidates），4 个 job 分阶段执行（build→deploy→finalize+notify→failure-alert），runtime-data 分支持久化，Pages 部署，故障告警
- 更新 README.md: 完整操作手册（本地搭建/测试/试运行/源配置/GitHub 配置/手动运行/自定义域名/模板/供应商扩展/故障行为）
- 更新 .gitignore: 新增 .venv/.local/build/runtime-data
- 新增 `test_workflow_contract.py`: 2 tests（定时+手动输入+并发控制 + Pages 权限）
- 扩展 `test_cli.py`: CLI 参数契约测试（验证 workflow 命令签名一致）
- 全量: 75/75 passed, ruff 遗留 4 个历史 issue（非本次引入）

## 项目总览 — 全部 Main Tasks 完成！

| Task | 内容 | 测试数 |
|------|------|--------|
| Source 1-7 | 来源子系统（35 源/适配器/去重/评分/健康/CLI） | 41 |
| Main 4 | Runtime State（JSON 存储+三阶段推进） | 2 |
| Main 5 | Provider Chain（供应商协议+退避链+AI 编辑） | 8 |
| Main 6 | Selection + Rendering（多样性+模板+静态站点） | 5 |
| Main 7 | Notification Gateway（MeoW/企微/Webhook） | 2 |
| Main 8 | Pipeline + CLI Stage Commands | 4 |
| Main 10 | GitHub Actions + CI/CD + Docs | 3 |
| **Total** | | **75** |

## Main Task 8: Pipeline + CLI Stage Commands — 完成

- 创建 `src/ai_news_sniffer/pipeline.py`: `Pipeline.build()` 完整 7 步管线（采集→编辑→多样性→去重→存储→渲染），支持 dry_run
- 扩展 `src/ai_news_sniffer/cli.py`: 新增 build/verify-url/mark-published/notify/notify-failure 命令，保留原有 sources 子命令
- 创建 `src/ai_news_sniffer/__main__.py`: `python -m ai_news_sniffer` 入口
- 新增 `tests/conftest.py` (settings fixture), `test_pipeline.py` (2 tests), `test_cli.py` (2 tests)
- 全量: 72/72 passed, ruff clean

## Main Task 7: Notification Gateway — 完成

- 创建 `src/ai_news_sniffer/notifications/__init__.py`、`base.py`、`meow.py`、`wecom.py`、`webhook.py`
- `NotificationChannel` 协议 + `RetryingChannel` 基类（指数退避重试 + 错误安全格式化）
- `build_channels` 工厂 + `send_all` 容错批量发送（单通道失败不影响其他）
- MeoW 通道: `https://api.chuckfang.com/{nickname}` + markdown params
- WeCom 通道: `msgtype:markdown` + `errcode` 校验
- Webhook 通道: 标准 JSON payload + `rendered_message`
- 测试: `test_notifications.py`（2 tests），全量 68/68, ruff clean

## Main Task 6: Template Rendering & Static Site — 完成

- Plan Task 6: `selection.py` — `select_diverse_events`（来源上限 2/类别上限 3 多样性约束）+ `build_degraded_events`（模型不可用时降级报告）
- Plan Task 7: `rendering/site.py` — 沙盒 Jinja2 (SandboxedEnvironment + StrictUndefined) 渲染器，路径穿越防护
- 5 模板文件: `report.html.j2`（日报正文+来源覆盖+运行提示）、`index.html.j2`（301 重定向到最新日期页）、`archive.html.j2`（历史归档索引）、`notification.md.j2`（通知消息模板）、`static/style.css`（移动优先紫色系）
- 新增测试: `test_selection.py`（2 tests）+ `test_rendering.py`（3 tests）
- 全量: 66/66 passed, ruff clean

## 合并到 main（本地）

- 修复 worktree `.git` 文件（macOS 路径 → Windows 路径）
- feature 分支 ref 丢失，手动从 reflog 重建 `a18547c`
- merge `bb1fe63` 到 main（--no-ff）：79 files, 5009 insertions
- 合并后全量测试: 75/75 passed ✅
- README.md 翻译为中文 + 新增 docs/DEPLOY.md 部署文档（9 章节，GitHub 密钥/Pages/权限配置、工作流详解、手动运行、自定义域名、5 类故障排查）
